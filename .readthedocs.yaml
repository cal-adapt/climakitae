version: 2

build:
  os: "ubuntu-22.04"
  apt_packages:
    - libproj-dev
    - libgeos-dev
  tools:
    python: "3.9"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - requirements: requirements.txt
