# Get Started

This page will become the 5-minute quickstart for the new core API.

## Planned content

- Installation and environment setup
- First `ClimateData()` query
- Basic processor usage (`time_slice`, `clip`)
- A minimal analysis/plot result

## Current recommendation

Use the new core entrypoint:

```python
from climakitae.new_core.user_interface import ClimateData
```

Legacy interfaces are still available for compatibility and are being documented with migration paths.
