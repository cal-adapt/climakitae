[project]
name = "climakitae-mcp"
version = "0.1.0"
description = "MCP server for climakitae code generation"
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.0"]

[project.scripts]
climakitae-mcp = "server:mcp.run"
